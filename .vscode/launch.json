{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "(Windows) Launch",
			"type": "cppvsdbg",
			"request": "launch",
			// NOTE: Must be run as admin for OHM to work properly
			"program": "${workspaceFolder}/build/vs/run/LCDHardwareMonitor.exe",
			"cwd": "${workspaceFolder}/build/vs/run",
			"logging": {
				"exceptions": false,
				"moduleLoad": false,
			}
		}
	]
}
