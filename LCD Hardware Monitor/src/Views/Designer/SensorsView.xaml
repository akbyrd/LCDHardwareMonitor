<UserControl x:Class="LCDHardwareMonitor.Views.SensorsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d" 
	d:DesignHeight="300" d:DesignWidth="300">

	<DockPanel>

		<!-- Title -->
		<TextBlock
			DockPanel.Dock="Top"
			Style="{StaticResource Title}"
			Margin="0,0,0,10"
			Text="Sensors" />

		<!-- Sensor List -->
		<ListView
			BorderBrush="{DynamicResource ButtonBorder}"
			BorderThickness="1">
			<!--ItemsSource="{Binding Drawables}">-->

			<ListView.Resources>
				<!-- Enable drag & drop on sensors -->
				<Style
					TargetType="ListViewItem"
					BasedOn="{StaticResource {x:Type ListViewItem}}">
					<EventSetter
						Event="PreviewMouseLeftButtonDown"
						Handler="Sensor_PreviewMouseLeftButtonDown" />
					<EventSetter
						Event="PreviewMouseLeftButtonUp"
						Handler="Sensor_PreviewMouseLeftButtonUp" />
					<EventSetter
						Event="MouseMove"
						Handler="Sensor_MouseMove" />
				</Style>
			</ListView.Resources>

			<ListView.ItemTemplate>
				<DataTemplate>
					<TextBlock
						Text="{Binding Name}" />
				</DataTemplate>
			</ListView.ItemTemplate>

			<TextBlock Text="Sensors View" />
		</ListView>
	</DockPanel>
</UserControl>