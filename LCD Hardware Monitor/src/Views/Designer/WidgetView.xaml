<UserControl x:Class="LCDHardwareMonitor.Views.WidgetView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d" 
	d:DesignHeight="300" d:DesignWidth="300">

	<StackPanel
		Orientation="Vertical"
		AllowDrop="True"
		DragEnter="OnDragEnter" DragLeave="OnDragLeave"
		DragOver="OnDragOver"   Drop="OnDrop"
		IsHitTestVisible="{Binding Path=EnableHitTest, RelativeSource={RelativeSource TemplatedParent}}">

		<StackPanel.ToolTip>
			<StackPanel Orientation="Vertical">
				<TextBlock Text="{Binding Name}" />
				<TextBlock Text="{Binding Identifier}" />
				<TextBlock Text="{Binding Position}" />
			</StackPanel>
		</StackPanel.ToolTip>

		<StackPanel.Resources>
			<!-- TODO: Animate -->
			<DropShadowEffect
				x:Key="dropShadowEffect"
				BlurRadius="8"
				Color="{DynamicResource AccentColor}"
				Opacity="{Binding Path=HighlightOpacity, RelativeSource={RelativeSource TemplatedParent}}" />
		</StackPanel.Resources>

		<!-- TODO: This should just show the drawable -->
		<TextBlock
			Text="{Binding Name}"
			HorizontalAlignment="Center">
		</TextBlock>
		<TextBlock
			Text="{Binding Drawable.Name}"
			HorizontalAlignment="Center" />

	</StackPanel>
</UserControl>