<UserControl x:Class="LCDHardwareMonitor.Pages.OHMDataPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:src="clr-namespace:LCDHardwareMonitor"
	DataContext="{Binding RelativeSource={RelativeSource Self}}"
	mc:Ignorable="d"
	d:DesignHeight="650" d:DesignWidth="1100">

	<Border
		BorderBrush="{DynamicResource ButtonBorder}"
		BorderThickness="1">
		<!-- Works -->
		<!--DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}}"-->

		<Grid>
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>

			<ScrollViewer Grid.Column="0">
				<!-- TODO: Implement INotifyCollectionChanged to support dynamic tree -->
				<TreeView
					ItemsSource="{Binding Root}">
					<TreeView.ItemTemplate>
						<HierarchicalDataTemplate
							DataType="{x:Type src:Node}"
							ItemsSource="{Binding Root.Nodes}">
							<TextBlock Text="{Binding Root.Text}" />
						</HierarchicalDataTemplate>
					</TreeView.ItemTemplate>
				</TreeView>
			</ScrollViewer>

			<ScrollViewer Grid.Column="1">
				<ListBox ItemsSource="{Binding Root.Nodes}" />
			</ScrollViewer>

			<ScrollViewer Grid.Column="2">
				<ListBox ItemsSource="{Binding stuff}" />
			</ScrollViewer>
		</Grid>

	</Border>
</UserControl>
